numbers = [0, 1]
for i in range(2, 200000):
    new = numbers[i-1] + numbers[i-2]
    numbers.append(new)

def Divisibility(m):
     for i in numbers:
        if i % m ==0 and i != numbers[0]:
            print(numbers.index(i))
            break
            


Divisibility(233328)
Divisibility(433156)
